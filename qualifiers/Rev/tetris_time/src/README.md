requires ncurse

To build and produce a stripped binary, run make from this directory. The resulting tetris_time binary will be placed alongside the source. To remove it, run make clean.

The flag is set via the FLAG variable at the top of the Makefile and is the only value that needs to be changed between deployments.
