# Backup Bounce Solution

## Intended path

1. Register and inspect the exported backup format.
2. Notice the restore format accepts server-component tokens as data.
3. Import a backup that references the internal flag preview component with the preview key.
4. Render the component into a visible profile field.

## Usage

```bash
python solve.py http://127.0.0.1:5000
```
